# Model Runtime

Status: accepted

Decision: AppSec Pilot is implemented as an independent product with original code, a policy-first agent architecture, scoped local/staging execution, and a local OpenAI-compatible model adapter.
